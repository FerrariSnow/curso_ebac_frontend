let multiplica = (a: number, b: number): string =>
    `A multiplicação de ${a} e ${b} é: ${a * b}.`;

console.log(multiplica(53, 35));

let cumprimenta = (nome: string): string =>
    `Olá, ${nome}, espero que esteja bem.`;

console.log(cumprimenta("Ferrari"));
